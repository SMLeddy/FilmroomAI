import { SidebarLayout } from '../components/SidebarLayout';

/**
 * Defines the layout structure for the individual report page.
 * It wraps the page content with the main application's SidebarLayout.
 */
export default [SidebarLayout];